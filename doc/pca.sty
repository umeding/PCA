\usepackage{palatino}
\usepackage[english]{babel} % English language/hyphenation
%\usepackage[protrusion=true,expansion=true]{microtype} % Better typography
\usepackage[activate={true,nocompatibility},final,tracking=true,kerning=true,spacing=true,factor=1100,stretch=10,shrink=10]{microtype}

\usepackage{amsmath,amsfonts,amsthm} % Math packages
\usepackage[svgnames]{xcolor} % Enabling colors by their 'svgnames'
\usepackage[hang, small,labelfont=bf,up,textfont=it,up]{caption} % Custom captions under/above floats in tables or figures
\usepackage{booktabs} % Horizontal rules in tables
\usepackage{siunitx}
\usepackage{fix-cm}	 % Custom font sizes - used for the initial letter in the document
\usepackage{float}
\restylefloat{table}

\usepackage{tikz}
\usetikzlibrary{shapes,arrows,shadows}
\usepackage[miktex]{gnuplottex}
\usepackage{pgfplots}

\usepackage{url}
\usepackage{acronym}
%\usepackage[style=altlist, nonumberlist, numberedsection, acronym, toc]{glossaries}
\usepackage{sectsty} % Enables custom section titles
\allsectionsfont{\usefont{OT1}{phv}{b}{n}} % Change the font of all section commands

\usepackage{fancyhdr} % Needed to define custom headers/footers
\pagestyle{fancy} % Enables the custom headers/footers
\usepackage{lastpage} % Used to determine the number of pages in the document (for "Page X of Total")

% Headers/footers:
% Create page numbering, alternating position
\renewcommand{\sectionmark}[1]{\markright{#1}}
\lhead{}
\chead{}
\rhead{}
\lfoot[\fancyplain{}{\thepage\ of \pageref{LastPage}}]         {\fancyplain{}{}}
\cfoot[\fancyplain{}{}]                 {\fancyplain{}{}}
\rfoot[\fancyplain{}{}]       {\fancyplain{}{\thepage\ of \pageref{LastPage}}}

\renewcommand{\headrulewidth}{0.0pt} % No header rule
\renewcommand{\footrulewidth}{0.4pt} % Thin footer rule

\usepackage{lettrine} % Package to accentuate the first letter of the text
\newcommand{\initial}[1]{ % Defines the command and style for the first letter
\lettrine[lines=3,lhang=0.3,nindent=0em]{
\color{DarkGoldenrod}
{\textsf{#1}}}{}}

\DeclareCaptionType{capequ}[Equation][List of equations]
\captionsetup[capequ]{labelformat=empty}

% small inline vector
\newcommand{\tvect}[2]{%
  \ensuremath{\Bigl(\negthinspace\begin{smallmatrix}#1\\#2\end{smallmatrix}\Bigr)}}

%----------------------------------------------------------------------------------------
%	TITLE SECTION
%----------------------------------------------------------------------------------------

\usepackage{titling} 
\newcommand{\HorRule}{\color{DarkGoldenrod} \rule{\linewidth}{1pt}} % Defines the gold horizontal rule around the title

%opening
% Horizontal rule before the title
\pretitle{\vspace{-30pt} \begin{flushleft} \HorRule \fontsize{50}{50} \usefont{OT1}{phv}{b}{n} \color{DarkBlue} \selectfont}
\title{Principal Components Analysis}
% Whitespace under the title
\posttitle{\par\end{flushleft}\vskip 0.5em}

% Author font configuration
\preauthor{\begin{flushleft}\large \lineskip 0.5em \usefont{OT1}{phv}{b}{sl} \color{DarkRed}}
\author{Uwe B. Meding}
% Configuration for things after author
\postauthor{\footnotesize \usefont{OT1}{phv}{m}{sl} \color{DarkGray}

uwe@uwemeding.com% Your institution

\par\end{flushleft}\HorRule} % Horizontal rule after the title

\date{} % Add a date here if you would like one to appear underneath the title block


% Setup the PDF properties
\pdfinfo{
  /Author (Uwe B. Meding, uwe@uwemeding.com)
  /Title (Principal Component Analysis)
  /Subject (Principal Component Analysis)
  /Keywords (Principal Component Analysis)
  /Producer (LaTex)
  /Creator (pdf2latex)
  /CreationDate (D:20130929000000)
  /ModDate (D:20130929000000)
}

